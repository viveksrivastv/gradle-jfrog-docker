buildscript {
  repositories {
    jcenter()
  }
}
allprojects {
  apply plugin: 'java'
  apply plugin: 'maven'
  group = 'org.jfrog.example.gradle'
  version = '1.1.2'
  status = 'release'
}
artifactoryPublish.skip = true
task wrapper(type: Wrapper) {
  gradleVersion = '2.4'
}
